package service;

import metier.Rectangle;

/*public class RectangleService implements service.Surface {

	@Override
	public double getArea() {
		Rectangle r = new Rectangle(r.getWidth(), r.getHeight());
		
		return r.getWidth() * r.getHeight();
	}

}*/
